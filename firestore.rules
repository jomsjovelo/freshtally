rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    function isSignedIn() { 
      return request.auth != null; 
    }
    
    function isSuperAdmin() {
      let p = /databases/$(database)/documents/userProfiles/$(request.auth.uid);
      return isSignedIn() && exists(p) && get(p).data.role == 'super_admin';
    }
    
    function isMemberOf(tId) {
      if (!isSignedIn()) return false;
      let p = /databases/$(database)/documents/userProfiles/$(request.auth.uid);
      let t = /databases/$(database)/documents/tenants/$(tId);
      
      // Dual-layered verification for resilience during indexing/propagation.
      // 1. Direct Ownership Check: Compares token email with tenant ownerEmail (case-insensitive fallback).
      // 2. Profile Check: Verifies tenantId stored in the user's profile document.
      
      let emailMatch = exists(t) && get(t).data.ownerEmail.toLower() == request.auth.token.email.toLower();
      let profileMatch = exists(p) && get(p).data.tenantId == tId;
      
      return emailMatch || profileMatch;
    }

    match /userProfiles/{userId} {
      allow read: if isSignedIn() && (request.auth.uid == userId || isSuperAdmin());
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow update: if isSignedIn() && (request.auth.uid == userId || isSuperAdmin());
    }

    match /platform_broadcasts/{broadcastId} {
      allow read: if isSignedIn();
      allow write: if isSuperAdmin();
    }

    match /subscription_plans/{planId} {
      allow read: if isSignedIn();
      allow write: if isSuperAdmin();
    }

    match /tenants/{tenantId} {
      allow create: if isSignedIn();
      allow read, update: if isSignedIn() && (isMemberOf(tenantId) || isSuperAdmin());
      
      match /{allSubcollections=**} {
        allow read, write: if isSignedIn() && (isMemberOf(tenantId) || isSuperAdmin());
      }
    }
  }
}